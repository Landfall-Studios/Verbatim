rootProject.name = 'verbatim'

// Link all modules for IDE discovery.
// Each module builds independently with its own ./gradlew - this file
// exists only so IntelliJ can find and index all modules in one window.
includeBuild 'core'
includeBuild 'hytale'
includeBuild 'neoforge'
includeBuild 'paper'
